{
  "name": "darkcord",
  "version": "0.0.0",
  "private": true,
  "description": "Comunicacao leve por texto, voz e compartilhamento de tela.",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "pnpm --filter @darkcord/web dev",
    "build": "pnpm --filter @darkcord/web build",
    "check": "pnpm --filter @darkcord/web build",
    "lint": "pnpm --filter @darkcord/web lint",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
